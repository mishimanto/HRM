import api from './api';
export const documentService={all:()=>api.get('/documents'),expiring:days=>api.get('/documents-expiring',{params:{days}}),upload:data=>api.post('/documents',data),verify:(id,document)=>api.put(`/documents/${id}`,{title:document.title,document_type:document.document_type,description:document.description,expiry_date:document.expiry_date,is_verified:true}),remove:id=>api.delete(`/documents/${id}`),download:async(id,name)=>{const r=await api.get(`/documents/${id}/download`,{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download=name;a.click();URL.revokeObjectURL(url);}};
